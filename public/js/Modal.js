
let megCreate = document.querySelectorAll("#meg_js");
let modalBtn = document.querySelectorAll('.modal_btn')
console.log(megCreate)

    
/* megCreate.forEach(meg => {
    meg.addEventListener('click', function (e){
        console.log('ok');
        if(modalBtn.style.display = none)
            modalBtn.style.display = flex;

        console.log('ok 2');
    });
});
 */
// Afficher le Modal pour créer une mise en garde.
/* $.ajax({
    type: "POST",
    url: "add_student.blade.php",
    success:
function(retour){
            console.log('ok !');
           alert("Données retournées : " + retour );
    }
}); */